<!DOCTYPE html>
<html>
<head>
  <title>Perfil</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body class="<%= tema === 'oscuro' ? 'dark' : 'light' %>">
  <h1>Perfil</h1>
  <p>Nombre: <%= user.nombre %></p>

  <h3>Carrito espiritual</h3>
  <ul>
    <% (carrito || []).forEach(s => { %>
      <li><%= s %></li>
    <% }) %>
  </ul>

  <a href="/add-sesion/Meditación Lunar">Añadir sesión</a>

  <form method="POST" action="/logout">
    <button>Salir</button>
  </form>

  <p><a href="/">Inicio</a></p>
</body>
</html>
